# 距离分档方案填写说明

距离分档用于根据检测到的人脸宽度，为小脸、中脸、大脸分别使用不同的打码模式和参数。

## 最小可用格式

整个输入必须是 JSON 数组。每个数组元素代表一个档位：

```json
[
  {
    "name": "medium",
    "min_width": 100,
    "max_width": 199,
    "modes": ["gaussian"],
    "face_grid_step": 12,
    "dot_radius": 2,
    "grid_n": 5
  }
]
```

## 字段说明

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | 字符串 | 档位名称，仅用于识别和展示，例如 `small`、`medium`、`large`。 |
| `min_width` | 整数 | 人脸框宽度下限，单位为像素，包含此值。 |
| `max_width` | 整数 | 人脸框宽度上限，单位为像素，包含此值。 |
| `modes` | 字符串数组 | 该档位使用的模式，数组顺序就是叠加顺序。 |
| `face_grid_step` | 整数 | `landmark_whole_face` 的网格间距，越小越密，通常 4-60。 |
| `dot_radius` | 整数 | landmark 红点半径，通常 1-10。 |
| `grid_n` | 整数 | 关键点周围网格层数，通常 3-11。 |

## 参数实际效果

- `min_width` / `max_width`：决定这一套方案用于多远的人脸。数值越小，通常代表人脸距离镜头越远；数值越大，代表人脸更近。它们只按原始检测框宽度分档，不会因为 `expand_ratio` 改变。
- `modes`：决定打码算法和叠加顺序。`gaussian` 会柔化细节，`pixelate` 会形成明显马赛克，`solid` 会直接盖住区域，landmark 模式会绘制红点遮罩。多个模式会依次处理同一张脸。
- `face_grid_step`：红点之间的间距。调小会更密、更难看到脸部细节，但会增加覆盖量；调大更稀疏，画面保留更多细节。
- `dot_radius`：每个红点的半径。调大红点更粗、遮挡更强，也更容易出现点连成片的效果。
- `grid_n`：关键点周围的密集网格尺寸。调大覆盖范围和遮挡强度增加，但画面更粗糙；调小则更轻量。

实验室顶部的全局参数还包括：

- `score_threshold`：人脸检测置信度门槛。调低会检测到更多疑似人脸，但误检可能增加；调高会减少误检，但可能漏掉远处或模糊人脸。
- `expand_ratio`：在检测框外扩的比例。调大能覆盖脸部边缘和发际线，但可能遮到背景；调小画面更自然，但边缘覆盖余量减少。
- `min_face_skip`：小于该脸宽的脸直接跳过。调大可以减少远景小脸的处理，调小或设为 `0` 可以覆盖更多小脸。

## 可用打码模式

- `landmark_whole_face`：整脸红点遮罩。
- `landmark`：只在关键点区域打码。
- `gaussian`：高斯模糊。
- `pixelate`：马赛克。
- `solid`：纯色遮挡。

模式可以叠加。例如先高斯模糊，再叠加马赛克：

```json
"modes": ["gaussian", "pixelate"]
```

## 推荐四档配置

可以按需要增加更多区间，单次请求最多支持 20 个区间；区间的 `min_width` 和 `max_width` 不应重叠。

```json
[
  {
    "name": "small",
    "min_width": 20,
    "max_width": 39,
    "modes": ["landmark"],
    "face_grid_step": 13,
    "dot_radius": 1,
    "grid_n": 2
  },
  {
    "name": "small",
    "min_width": 40,
    "max_width": 60,
    "modes": ["landmark"],
    "face_grid_step": 12,
    "dot_radius": 1,
    "grid_n": 3
  },
  {
    "name": "medium",
    "min_width": 60,
    "max_width": 80,
    "modes": ["landmark_whole_face"],
    "face_grid_step": 12,
    "dot_radius": 2,
    "grid_n": 4
  },
  {
    "name": "medium",
    "min_width": 80,
    "max_width": 110,
    "modes": ["landmark_whole_face"],
    "face_grid_step": 12,
    "dot_radius": 2,
    "grid_n": 5
  }
]
```

## 如何判断命中档位

系统使用原始检测框的人脸宽度 `face.w` 匹配：

- `40 <= face.w <= 59` 命中 small。
- `60 <= face.w <= 79` 命中 medium。
- `80 <= face.w <= 110` 命中 large。

上下限都包含。建议各档区间首尾相接，不要重叠，也不要留下空档。

如果同一张图片有多张脸，每张脸会独立匹配自己的档位。

## 全局模式与分档模式的关系

- `face_profiles` 为空：所有人脸使用全局模式和全局参数。
- `face_profiles` 不为空：命中档位的人脸使用该档位的 `modes` 和参数。
- 未命中任何档位的人脸，回退使用全局模式和全局参数。

因此，如果想让所有人脸都使用实验室顶部选择的模式，请清空距离分档输入框，填写 `[]`。

## 在页面中的操作

1. 进入“打码实验室”。
2. 点击“填入四档示例”，或直接粘贴自己的 JSON。
3. 修改 `min_width`、`max_width`、`modes` 和参数。
4. 点击“执行打码”查看原图与结果，并查看人脸检测置信度对比。
5. 确认效果后点击“同步到全局”，让后续新请求使用该方案。

## 常见错误

- 忘记最外层的 `[` 和 `]`，导致不是数组。
- 使用中文模式名称，例如 `"高斯模糊"`，应使用 `"gaussian"`。
- `min_width` 大于 `max_width`，该档位不会按预期命中。
- 多个档位区间重叠，匹配结果会取第一个命中的档位。
- 只修改了实验室参数但没有点击“同步到全局”，不会影响普通 API 请求。
