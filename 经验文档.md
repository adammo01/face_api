# 人脸打码项目开发经验与大型项目完整流程

> 本文根据本项目从需求调整、Bug 排查、本地验证、线上发布到文档同步的实际过程整理。
> 目标不是记录某一次修改，而是总结一套可以迁移到大型项目的工作方法。
> 文档日期：2026-08-08

## 一、先理解项目，再开始改代码

大型项目最容易犯的错误，是看到一个页面问题就直接改某一行。正确顺序应该是先回答：

1. 用户真正想改变的行为是什么？
2. 这个行为由哪些模块共同完成？
3. 当前代码的真实入口、数据流和部署版本是什么？
4. 修改会影响哪些旧业务、接口和数据？
5. 怎样证明修改成功？

以本项目的“实验室按钮没有反应”为例，完整链路是：

管理页按钮 -> 标签切换 -> iframe 加载实验室 -> 实验室 JavaScript 初始化 -> URL 或文件输入 -> POST /api/lab/test -> 图片下载 -> YuNet 检测与打码 -> 结果回到页面 -> 图片预览。

只检查其中一段，很容易得到“接口正常但页面坏了”或“页面正常但服务器下载失败”的错误结论。

## 二、本次项目最重要的经验

### 1. 把需求转换成可验收的行为

“图片库要翻页”应该转换成：

- 图片库独立标签页，不打开新页面。
- 默认每页 10 张。
- 可选 10、20、50、100、200 张。
- 上一页、下一页、页码跳转都正常。
- 点击图片打开大图预览。
- 点击卡片其它区域仍能定位任务。

“请求记录默认 20 条”要同时检查：

- 下拉框的选中项是否为 20。
- JavaScript 初始变量是否为 20。
- 首次请求是否真的发送 limit=20。

本项目曾经出现下拉框显示 20、脚本变量仍为 10 的问题。页面看起来正确，但首次请求实际仍是 10。这类界面状态和真实状态不一致，是前端常见 Bug。

### 2. HTML 结构错误会连锁破坏 JavaScript

实验室曾把内容放在 head 内，并在 DOM 元素创建之前绑定事件：

    const drop = document.getElementById("lab-drop");
    drop.addEventListener(...);

此时 drop 是 null，脚本直接中断，后面的 labTest 也没有定义，所以点击按钮完全没有反应。

排查这类问题时，浏览器控制台比猜测更可靠。最终错误包括：

    ReferenceError: labTest is not defined
    SyntaxError: Invalid or unexpected token

解决方法：

- 保证 head、body 结构完整。
- DOM 事件绑定放到 DOMContentLoaded 后。
- 页面脚本出现语法错误时，整个脚本可能都不会执行。
- 内嵌 iframe 要单独检查 iframe 自己的控制台和请求。

### 3. 三层验证比单一测试可靠

第一层是静态检查：

- Python 使用 py_compile。
- JavaScript 提取 script 后使用 node --check。
- 检查关键 HTML 标签是否成对。
- 使用 git diff --check。

第二层是接口测试：

- /healthz 返回 model_loaded=true。
- 管理接口正确鉴权。
- /api/lab/test 返回结果图。
- 无效 URL 返回可读错误。
- 真实图片 URL 返回 HTTP 200。

第三层是浏览器验收：

- 标签可以切换。
- 设置字段显示并保存。
- 实验室按钮触发请求。
- 错误显示在页面上。
- 结果图片出现。
- 点击图片打开预览层，Esc 关闭。

本项目通过浏览器复现后才发现实验室脚本语法错误；通过服务器本机调用后才发现图片 URL 下载受代理影响。单靠单元测试不容易发现这两类问题。

### 4. 错误不能被吞掉

不好的错误处理是空的 catch。它会让用户看到“没有反应”，也让开发者无法定位问题。

好的错误处理至少包括：

- 捕获错误。
- 显示用户能理解的错误。
- 服务端日志保留原始异常。
- 根据 HTTP 状态区分鉴权、输入、下载和处理错误。

实验室前端还曾把所有 HTTP 响应都当成功 JSON 处理。正确做法是先检查 response.ok，再读取 detail 或 message。

### 5. 远程图片下载有网络环境风险

同一个图片 URL，curl 直连可以返回 200，Python requests 却可能因为继承 HTTP_PROXY 或 HTTPS_PROXY，在代理 TLS 握手处超时。

因此服务端图片下载 Session 应明确配置网络行为：

    session.trust_env = False

实验室验证第三方 CDN 时，还使用了单连接和更长读取超时，避免 Range 分段下载被 CDN 限流。生产下载还应限制协议、目标地址、最大大小、连接超时、读取超时、重定向次数，并防止 SSRF。

### 6. 页面结构要和功能边界一致

管理页应该形成清晰边界：

- 概览：指标、请求记录、概览图片库。
- 图片库：独立分页和大图预览。
- 任务详情：任务 ID、输入图、结果图、完整请求 JSON、完整响应信息。
- 全局设置：并发、重试、TTL 和打码默认参数。
- 实验室：上传或 URL 输入、参数调试、结果预览。

如果请求记录被意外放进设置或实验室，用户会认为页面串了。这不是 CSS 小问题，而是组件边界错误。

### 7. 增量发布比重新部署更安全

本次线上发布只上传修改后的 app.py，没有覆盖线上数据库、图片目录、管理员 Token、模型文件或 Tunnel 配置。

发布前必须确认远程目录、systemd 服务名、worker 数量、环境变量和数据目录，然后执行：

上传文件 -> 远程 py_compile -> 重启服务 -> systemctl is-active -> 本机 healthz -> 公网 healthz -> 真实 API 冒烟测试。

任何一步失败，都不能宣布上线成功。

## 三、大型项目的完整开发流程

### 阶段 0：建立项目基线

开始前记录：

- Git 分支和提交。
- 当前线上版本。
- 本地运行方式。
- 依赖和 Python 版本。
- 数据库、文件存储和队列位置。
- 现有测试命令。
- 关键环境变量名称，不记录实际密钥。

同时检查工作区脏文件。不要随意删除、回退或覆盖用户已有修改。

### 阶段 1：需求澄清与范围定义

把自然语言需求写成用户故事、输入输出、正常流程、错误流程、权限边界、数据保留规则和验收标准。

大型需求要拆成可以独立验证的小目标：

1. 标签切换正确。
2. 分页状态正确。
3. 任务详情完整。
4. 图片预览正确。
5. 全局设置保存正确。
6. 实验室请求和错误反馈正确。

### 阶段 2：代码探索与架构确认

先查入口，再查调用链：

路由 -> 参数模型 -> 业务服务 -> 数据库或文件 -> 响应。

页面按钮 -> JavaScript 函数 -> API -> DOM 更新。

部署脚本 -> 远程目录 -> systemd -> 公网域名。

优先使用 rg 查找路由、变量、事件处理函数和环境变量，再阅读上下文，不要只看文件名猜结构。

### 阶段 3：设计最小改动方案

每行修改都应该能回答“它对应哪个需求”。

- 优先复用已有 API 和样式。
- 不为一次性需求创建复杂抽象。
- 不顺手重构无关模块。
- 改接口时先考虑旧调用方兼容性。
- 修改鉴权或 Token 前确认旧业务依赖。

### 阶段 4：先写复现测试

Bug 修复遵循：

先证明现在会失败 -> 写最小回归测试 -> 修改实现 -> 测试变绿 -> 真实浏览器和接口验证。

测试要覆盖正常输入、空输入、错误输入、权限失败、边界分页、空数据库、外部超时、重复请求和并发。

### 阶段 5：实现与分层验证

每完成一个小改动就验证一次。推荐顺序：

1. 格式、语法和类型检查。
2. 单元测试。
3. 集成测试。
4. API 冒烟测试。
5. 浏览器验收。
6. 性能和安全检查。

### 阶段 6：代码审查

重点检查：

- 是否破坏旧接口。
- 是否引入未鉴权入口。
- 是否存在 SSRF、注入或敏感头泄露。
- 是否吞掉异常。
- 是否存在并发竞态。
- 是否有无上限缓存、文件或日志。
- 是否更新相关文档和测试。

### 阶段 7：提交与发布

提交做到：

- 一个提交表达一个完整主题。
- 提交信息说明行为变化。
- 不提交 Token、密码、env、虚拟环境和备份文件。
- 推送前查看 diff stat 和 status。

生产发布采用：

本地 -> 预发布 -> 小流量或灰度 -> 全量 -> 监控 -> 文档同步。

没有预发布环境时，至少保留可回退提交、发布前后健康检查、远程日志和回滚命令。

### 阶段 8：上线后观察

发布后观察：

- 健康检查。
- 5xx 比例。
- 下载失败比例。
- 平均和 P95 延迟。
- CPU、内存、文件数量和数据库大小。
- 管理页关键流程。
- 任务是否持续产生完整记录。

## 四、本项目日常检查清单

### 开发前

- [ ] 需求已转换为验收标准。
- [ ] 已确认线上和本地版本。
- [ ] 已查看工作区脏文件。
- [ ] 已找到真实路由和数据流。
- [ ] 已确认是否涉及 Token、个人数据或线上数据。

### 修改后

- [ ] py_compile 和 JavaScript 语法检查通过。
- [ ] 相关单元测试通过。
- [ ] 错误路径有可读反馈。
- [ ] 空数据和分页边界已验证。
- [ ] 浏览器控制台无新增错误。
- [ ] 真实图片 URL 至少成功测试一次。

### 发布前

- [ ] git diff --check 通过。
- [ ] 没有提交密钥、备份和虚拟环境。
- [ ] 已确认远程目录和服务名。
- [ ] 已决定增量发布还是完整部署。
- [ ] 已准备回滚方式。
- [ ] 已更新 README 或对接文档。

### 发布后

- [ ] systemctl is-active 为 active。
- [ ] 本机 healthz 返回模型已加载。
- [ ] 公网 healthz 返回 200。
- [ ] 真实 API 返回预期结构。
- [ ] 管理页能加载数据。
- [ ] 任务详情和图片预览可用。
- [ ] 线上日志无持续异常。

## 五、经验总结

1. 页面能打开，不等于功能可用；必须验证按钮、请求、响应和结果。
2. 接口返回 200，不等于用户流程成功；页面脚本、iframe、DOM 和浏览器错误同样重要。
3. URL 能在浏览器打开，不等于服务器一定能下载；代理、TLS、超时和 CDN 限流都会改变结果。
4. 不要只修现象；找到 labTest is not defined 后，才能继续定位脚本语法错误。
5. 不要用全量覆盖代替增量发布；数据库、图片、Token 和配置必须明确保护。
6. 不要让错误静默消失；反馈越早越具体，排查成本越低。
7. 文档不是收尾工作，而是发布的一部分；代码、测试、部署和文档必须描述同一个真实系统。
8. 大型项目靠流程降低风险：需求可验收、改动可追踪、测试可重复、发布可回滚、运行可观测。

### 9. 轻量配置优先使用浏览器本地预设

实验室的自定义参数预设只服务于当前操作人员，不需要参与服务端任务数据，因此使用 `localStorage` 比新增数据库表更简单。实现时要注意：

- 预设保存完整参数，而不是只保存当前模式，避免恢复后出现半套配置。
- 应提供覆盖、删除和恢复默认值，避免用户被错误配置锁住。
- 预设名称必须经过 HTML 转义，不能直接拼接进 `select.innerHTML`。
- 本地预设不能替代全局设置；需要影响后续 API 请求时，仍要显式执行同步操作。

## 六、个人学习模板

每次接到开发任务，可以照下面五句话执行：

我先确认需求最终要改变什么行为。
我再找到入口、调用链和数据边界。
我为失败场景写一个最小复现。
我先本地验证，再用真实环境做冒烟测试。
我最后发布、观察、记录并更新文档。

这套流程看似慢，实际上能避免反复上线、靠猜测排查和修复一个问题又引入另一个问题。
