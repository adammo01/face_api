# 拓飞云服务器 (36.133.106.162) — 优化分析报告

> 分析时间: 2026-08-06 21:55 CST
> 服务器: 16 核 / 31GB RAM / Ubuntu 6.8

---

## 一、问题一：并发上限设置 80 失败

### 根因：代码硬限制 + 系统层瓶颈

#### 1.1 代码层面 — 三处硬编码上限 32

`app.py` 中并发上限被**三处硬编码**限制在 32，无论通过环境变量还是 Admin API 都无法突破：

| 位置 | 行号 | 限制 |
|------|------|------|
| `_set_settings()` | 241 | `"max_concurrent_tasks": (1, 32, int)` |
| `AdminSettingsRequest` (Pydantic Model) | 972 | `Field(default=None, ge=1, le=32)` |
| `_get_int_setting()` | 223 | `max_value=32` |

当前 DB 中实际值已经是 32（天花板）：
```
max_concurrent_tasks = 32   (updated 2026-08-06T13:53:48)
```

**结论：设置 80 时被 Pydantic 校验直接拒绝（422 Validation Error）。**

#### 1.2 系统层面 — 即使去掉代码限制也跑不到 80

| 检查项 | 当前值 | 80 并发是否够 |
|--------|--------|--------------|
| 文件描述符软限制 (`ulimit -n`) | **1024** | ❌ 严重不足 |
| uvicorn workers | **1 个** | ❌ 16 核 CPU 闲置 |
| `somaxconn` | 4096 | ⚠️ 勉强 |
| `ip_local_port_range` | 32768-60999 (~28k) | ✅ |
| `tcp_max_syn_backlog` | 8192 | ✅ |
| 内存 (31GB total, 28GB available) | 充足 | ✅ |

**关键瓶颈**：软 fd 限制 1024，每个 HTTP 连接 + DB 连接 + 静态文件读至少需要 3-5 个 fd。80 并发 + 正常业务开销很快就会打满 1024。

---

## 二、问题二：打码方案优化加速

### 2.1 当前性能基线

| 指标 | 值 |
|------|-----|
| 总请求 (24h) | 3,376 |
| 成功率 | 99.26% |
| **平均耗时** | **2,864 ms** |
| 照片下载 | 1,192 ~ 2,335 ms |
| 人脸检测+打码 | 139 ~ 434 ms |

### 2.2 耗时构成分析

```
每个请求总耗时 ≈ 2.9 秒
├── 图片下载:      ~1,700 ms  (58%)  ← 最大瓶颈
├── 人脸检测:      ~250 ms   (9%)
├── 打码/编码:     ~50 ms    (2%)
├── DB 写入:       ~50 ms    (2%)
└── 其他开销:       ~850 ms   (29%)
```

**图片下载占了接近 60% 的耗时**，远大于实际打码计算。

### 2.3 优化方案（按优先级排序）

#### 🔴 P0 — 立即见效（无需改代码）

**1. 增加 uvicorn workers（16 核 → 当前只用 1 核）**

当前：
```
ExecStart=uvicorn app:app --host 0.0.0.0 --port 8000 --workers 1
```
改为：
```
ExecStart=uvicorn app:app --host 0.0.0.0 --port 8000 --workers 8
```
- 预期效果：吞吐量翻倍（8x 进程并行处理 CPU-bound 任务，绕开 GIL）
- 风险：内存从 ~2.4GB 增到 ~4GB（每个 worker 加载 YuNet 模型约 40MB）

**2. 提高文件描述符限制**

```bash
# /etc/security/limits.conf 或 systemd service
LimitNOFILE=65535
```

**3. systemd service 增加资源限制**

```ini
[Service]
LimitNOFILE=65535
LimitNPROC=65535
```

#### 🟡 P1 — 小幅改代码（1-2 小时内可完成）

**4. 提高并发上限校验值（三处硬编码 32 → 128）**

修改 `app.py`：

```python
# 行 241: _set_settings() 的 allowed dict
"max_concurrent_tasks": (1, 128, int),

# 行 223: _get_int_setting() 默认 max_value
max_value: int = 128

# 行 972: AdminSettingsRequest Pydantic Field
max_concurrent_tasks: Optional[int] = Field(default=None, ge=1, le=128)

# 行 102: 默认值也可调高
DEFAULT_MAX_CONCURRENT_TASKS = int(os.environ.get("FACE_BLUR_MAX_CONCURRENT_TASKS", "32"))
```

**5. 图片下载优化：连接复用 + 超时优化**

当前 `_download()` 每次新建连接（`urllib.request.urlopen`），高并发下开销大。建议改用 `requests.Session` 或 `httpx.AsyncClient` 做连接池复用。

**6. 图片预处理加速：先 resize 再检测**

当前对大图（4-5MB, 4000px+）直接送入 YuNet 做多尺度检测。可以先 limit 到 1920px 长边：
- 预期效果：检测耗时降低 30-50%
- 风险：极小人脸可能漏检（但打码场景下人脸通常不小）

#### 🟢 P2 — 架构优化（需要更多改造）

**7. 下载+检测流水线化**

当前是严格的同步流程：下载 → 检测 → 打码 → 返回。下载期间 CPU 完全空闲。
可以改为：
- 异步下载（asyncio/httpx）
- 下载完成立即送检测队列
- 多个 worker 并行消费

**8. 结果缓存**

相同的 `image_url` 如果短时间内被多次请求，可以缓存打码结果（基于 URL hash）。
当前 DB 里已经有 `image_url` 字段，加一个简单的 LRU 缓存即可。

**9. 接入 GPU（如果有条件）**

YuNet ONNX 可以跑 ONNX Runtime GPU 后端，检测速度可提升 5-10 倍。但当前服务器无 GPU。

---

## 三、当前运行状态概览

```
PID:        149996
Workers:    1 (--workers 1)
Uptime:     从 09:30 运行至今 (~12.5h)
CPU:        17.1% (单个进程)
内存:       2.4 GB RSS (峰值 6GB, 说明有内存泄漏)
并发设置:   32 (手动改到了上限)
线程数:     54 (uvicorn 默认线程池)
静态文件:   2,205 个 / 1.96 GB
```

⚠️ **注意**：systemd journal 显示昨天 (8/6 02:33) 有**连续 10 次启动失败**（exit-code 1），后来手动修复后恢复。峰值内存曾达 6GB，当前 2.4GB — 说明可能有内存泄漏或 GC 未及时回收。

---

## 四、推荐执行顺序

| 步骤 | 操作 | 预期收益 | 风险 |
|------|------|---------|------|
| 1 | systemd 增加 `LimitNOFILE=65535` + `--workers 8` | 吞吐量 3-5x | 低 |
| 2 | 改三处硬编码 32 → 128 | 支持 >32 并发 | 低 |
| 3 | Admin API SET max_concurrent_tasks=64 | 生效 | 低 |
| 4 | 图片下载改用连接池 (httpx) | 下载耗时 -30% | 中 |
| 5 | 大图 resize 预处理 | 检测耗时 -30% | 低 |
| 6 | 加 URL 级 LRU 缓存 | 重复请求 0ms | 低 |

---

## 五、具体修改文件

### 5.1 `/etc/systemd/system/faceblur.service`

```ini
[Service]
LimitNOFILE=65535
# workers 从 1 → 8（16 核机器的一半，留余量给系统）
ExecStart=/root/faceblur/venv/bin/uvicorn app:app --host 0.0.0.0 --port 8000 --workers 8 --limit-max-requests 5000
```

增加 `--limit-max-requests 5000` 让每个 worker 处理 5000 个请求后自动重启，防止内存泄漏累积。

### 5.2 `/root/faceblur/app.py` — 三处改动

```python
# 行 241: allowed dict -> 128
"max_concurrent_tasks": (1, 128, int),

# 行 223: 默认 max_value -> 128
def _get_int_setting(key: str, default: int, min_value: int = 1, max_value: int = 128)

# 行 972: Pydantic Field -> le=128
max_concurrent_tasks: Optional[int] = Field(default=None, ge=1, le=128)
```
